# name: python-data-pipeline
# description: Use when working with data ingestion, transformation, or export tasks in this Python project.

## When to use
Invoke this skill for any ETL, data cleaning, or pipeline task.

## Steps
1. Check existing pipeline patterns in `src/pipelines/`
2. Follow the base class in `src/base.py`
3. Run `pytest tests/` to validate
4. Update `docs/pipelines.md`